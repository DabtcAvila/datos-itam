{
  "name": "datos-itam-tests-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for datos-itam.org observatory (Playwright against production).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
